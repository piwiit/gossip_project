<!DOCTYPE html>
<html lang="fr">
  <head>
    <meta charset="utf-8">
    <title>The Gossip Project</title>
    <!-- On peut avoir d'autres méta-données ici -->
  </head>  
  <body>
  <header>

  </header>

    <h1>Page d'accueil</h1>
      <p>Bienvenue <%= params["id"]%> !
      <br/>
      <br/> 
      <div class="card">
        <div class="card-header">
          Quote
        </div>
        <div class="card-body">
          <blockquote class="blockquote mb-0">
            <p><%= @gossip.content %></p>
            <footer class="blockquote-footer"><%= @gossip.user[:first_name] %><cite title="Source Title">  <%= @gossip.created_at %></cite></footer>
            <%= link_to "profile du createur", user_path(@gossip.user.id), :class =>"btn btn-secondary" %>
          </blockquote>
        </div>
      </div>
  </body>
      <p><%= link_to "Retour !", root_path, :class =>"btn btn-secondary" %></p>
</html>

 
